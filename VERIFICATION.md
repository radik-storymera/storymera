# Результаты проверки — 15 сентября 2026

## Сборка и автоматические тесты

- Установлены 21 пакет проекта, аудит npm: 0 известных уязвимостей на момент установки.
- Vite 7.3.6: поддерживает Node.js `^20.19.0 || >=22.12.0`; установленный 24.19.0 совместим.
- `npm.cmd test`: 4 теста, 4 успешных, 0 ошибок.
- `npm.cmd run build`: TypeScript и production-сборка успешны; результат в `dist`.
- npm сообщил о неразрешённом postinstall esbuild. Дополнительное разрешение скрипта не включалось: доступный пакет esbuild успешно выполнил production-сборку.

## Проверка в браузере Codex

- Каталог: одна карточка, Start story; после чтения — Continue story.
- Обе ветки пройдены через интерфейс: кафе и прогулка, общая сцена с правильным дополнительным текстом, пятая сцена, демонстрационный экран и бонусное продолжение.
- Перезагрузка восстановила сцену кафе; после возврата в каталог и Continue story восстановилась общая сцена с выбором прогулки, в том числе после перезагрузки.
- Restart chapter: отмена сохранила сцену, подтверждение вернуло начало.
- Ширина 1440: проверены каталог и проигрыватель, у проигрывателя две колонки по 619,2 px; горизонтального переполнения нет.
- Ширина 390: проверены каталог и проигрыватель; одна колонка, изображение перед текстом. Кнопки выбора одинаковой ширины 283,6 px, расположены одна под другой (Y=852,125 и 915,125). После выбора scrollY=0. Горизонтального переполнения нет.
- Проверены визуальные снимки каталога на ПК и проигрывателя на ПК/телефоне. Один запрос полного мобильного снимка завершился ошибкой инструмента; обычный снимок мобильной сцены получен успешно. Один клик проверки завершился тайм-аутом инструмента, после проверки текущего состояния повторный клик выполнен успешно.
- В журнале браузера за проверку нет ошибок и предупреждений приложения.
- После проверки тестовый прогресс сброшен через интерфейс.

## Границы проверки

- Повреждённые/устаревшие сохранения и изоляция сброса проверены автоматическими тестами, а не изменением localStorage через инструменты браузера.
- `prefers-reduced-motion` реализован в CSS; отдельная эмуляция этой настройки не выполнялась.
- Видео поддерживается типами данных и компонентом, но настоящего видеофайла нет: воспроизведение не проверено.
- Проверка выполнена в одном браузере; полноценная матрица Safari/Firefox/Android/iOS не выполнялась.
- Никакой защиты платного контента нет. localStorage и демонстрационный экран не заменяют серверные права доступа.

Локальный сервер: `http://localhost:5173/`, привязка `localhost`, публикация не выполнялась.

## Этап 1.1 — компактный проигрыватель

- Шапка 68 px на ПК, 64 px на телефоне. Верх сцены на ПК — 130 px.
- Заголовок 35 px на ПК, 27 px на телефоне; основной текст не уменьшен.
- Фото на ПК ограничено доступной высотой окна, object-fit: contain. На телефоне высота следует пропорциям изображения.
- Подпись: Your progress is saved on this device.
- Production-сборка успешна.
- Браузер: 1440×900, 1366×768, 390×844. Кадр полный, изображения загружены, горизонтального переполнения нет. На ПК короткая сцена заканчивается на 880,8 / 748,8 px соответственно, кнопка видна. На телефоне кнопка доступна обычной прокруткой страницы.
- Длинный текст проверен на временной отдельной странице с тем же CSS и 24 абзацами на всех трёх размерах: страница растёт, кнопка достижима, overflow-y текстовой колонки = visible, горизонтального переполнения нет. Тестовая страница удалена; текст истории и сохранение не менялись.

## Этап 1.2 — пропорции и компактная главная

Контент ограничен 1160 px. Медиа проигрывателя учитывает реальное соотношение сторон изображения (onLoad) или видео (onLoadedMetadata), доступную высоту окна и предел 460 px. Текстовый блок целиком центрируется по вертикали только на ПК, отступы 36 px. Сюжет и данные прогресса не изменялись.

Каталог: заголовок Stories, карточка 40/60, имя 46 px, описание и основная кнопка. Убраны повторяющиеся слоганы и плашки. Высота карточки на ПК 441,6 px; лицо и причёска сохранены при лёгком кадрировании.

Проверены 1366×768, 1440×900, 390×844. Ширина портретного медиа на ПК 389,3 / 460 px; полный кадр, contain. Горизонтального переполнения нет. Кнопка каталога заканчивается на 421,9 px на ПК и 741,4 px на телефоне. Start story и Continue story работают. При длинном тексте (временная отдельная страница с 24 абзацами) на всех размерах кнопка доступна обычной прокруткой, overflow-y колонки visible. Временная страница удалена. Production-сборка успешна. Скриншоты главной и проигрывателя показаны в ходе проверки. Видеофайла нет, воспроизведение не проверено.
